#include <stdio.h>
/**
 * main - Entry point
 * Return: 0 (Success)
 */
int main(void)
{
	int num;

	for (number = 0; number <= 9; num++)
	{
		putchar(number + '0');
	}
	putchar('\n');
	return (0);
}
